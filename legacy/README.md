# 同步生成恢复点

`sync-bff78a3.zip` 是 Git 提交 `bff78a3a9ff73c2db0d6ac32c47231bb63234657` 的 src、public、包及构建配置快照。包含完整同步执行、Store、UI 和数据契约，不包含密钥或用户数据。此目录不在 src/public 中，不会进入页面构建或运行时加载。

恢复前先备份当前数据库与代码。在独立分支比较快照或上述 Git 提交，恢复时必须同时处理任务启动/超时及 UI，不要仅替换请求函数。旧同步版本不理解 asyncGeneration 字段，不能直接用旧启动恢复覆盖仍有远端 ID 的任务，也不能重新提交这些任务。先导出/保存异步任务结果，或保留仅查询、下载兼容路径，再切换回同步执行。

src/lib/api.ts 和 openaiCompatibleImageApi.ts 保留原有同步能力及测试作为参考；新正式执行链不导入这两个文件。不要通过页面隐藏开关偷偷回退到付费同步重发。
